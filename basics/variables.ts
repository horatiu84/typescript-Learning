// let moiveTitle: string = "Terminator 2";
// moiveTitle = "Avatar";

// let myBoolean: boolean = true;
// myBoolean = false;

// let numCatLives: number = 9;
// numCatLives--;

const movies = ["Arrival", "The thing", "Amadeus"];

let foundMovie: string;

for (let movie of movies) {
  if (movie === "Amadeus") {
    foundMovie = "Amadeus";
  }
}
